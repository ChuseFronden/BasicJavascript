// 09.01 JavasScript code

var attendees = [];	        // Creates an empty array

console.log(attendees.length);  // Prints 0

console.log("=== Attendees ===");

attendees.push("Pekka");        // Adds a new element to the end of the array

attendees.push("Minna");        // Adds a new element to the end of the array

attendees.push("Heikki");	        // Adds a new element to the end of the array

attendees.push("Anna");

attendees.push("Jukka");

attendees.push("Iida");	        // Changes the value of the array element


for (var i = 0; i < attendees.length; i++) {
    console.log (attendees[i]); 	
}





